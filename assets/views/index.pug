extends ../common/pug/layout

block variables
  - var pageInfo = {title: 'index', description: 'index'}

block content
  body
    .wrapper
      //- +s-header()
      +s-default({
        title: "Our products",
        slot: "l-products"
      })
      //- +s-footer()
    +scripts()