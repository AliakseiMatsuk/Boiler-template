mixin b-description(o = {})

  +b.b-description
    +e.inner
      +e.content !{o.content}
      +e.image-wrapper
        img.image(src=`./images/${o.image.src}.jpg`, alt=`${o.image.alt}`, data-object-fit="cover", data-object-position="center")