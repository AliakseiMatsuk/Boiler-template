mixin l-tools(o = {})

  +b.UL.l-tools
    each tool in o.visibleTools
      - var {logo, alt} = lTools.tools[tool]
      +e.tool
        +e.logo(src=`${ROOT_FOLDER}/${IMAGES_FOLDER}/tools/${logo}.jpg` alt=`${alt}`)
