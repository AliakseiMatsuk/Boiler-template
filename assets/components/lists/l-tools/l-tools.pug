mixin l-tools(o = {})

  +b.UL.l-tools(role="list")
    each tool in o.visibleTools
      - var {logo, alt} = lTools.tools[tool]
      +e.tool
        +e.logo(src=`./images/tools/${logo}.jpg`, alt=`${alt}`)