mixin l-ordered(o = {})

  +b.OL.l-ordered(role="list")
    each item in lOrdered[o.list]
      +e.item(data-index=`${item.index}`)
        if item.title
          +e.title #{item.title}

        if item.description
          +e.description #{item.description}